---
title: Architecture
description: How the Graft core, SQLite integration, CLI, repository layer, and remotes fit together.
---

This page is for contributors and advanced debugging. Product integrations
should normally use the CLI, JSON output, Node.js/Electron SDK, and config
reference.

## Layers

```text
Application
  ordinary SQLite connection -> physical worktree database
  graft CLI / JSON
  Node.js / Electron SDK

graft-sqlite
  repository command service
  physical SQLite snapshot/import boundary

graft
  repository model
  objects, refs, index, merge, sync
  storage volumes, logs, snapshots
  remote backends

Remote storage
  fs, S3-compatible, Graft HTTP protocol
```

## Core Crates

| Crate | Role |
| --- | --- |
| `graft` | Core storage, repository objects, refs, index, merge, sync, remotes. |
| `graft-sqlite` | Repository command service and physical SQLite snapshot/import boundary. |
| `graft-cli` | CLI that maps arguments to repository operations. |
| `graft-test` | Integration test harnesses and workloads. |
| `graft-tracing` | Tracing setup helpers. |

## CLI Flow

CLI repository commands call the typed repository service directly:

```text
CLI arguments
  -> RepositoryCommand parser
  -> repository-scoped runtime/session
  -> repository operation
```

No SQLite connection or PRAGMA is used for that control flow. `graft sql --db
<path>` separately opens the physical database with ordinary SQLite.

## Commit Flow

```text
SQLite commits to a physical database (main file and possibly WAL)
  -> graft add takes a consistent private SQLite backup
  -> unchanged pages are reused and changed pages extend Graft storage
  -> the index records the resulting snapshot descriptor
  -> graft commit writes a tree and commit object
  -> branch ref moves to the new commit
  -> SQLite snapshots are materialized back to physical worktree files
```

Physical import can capture databases with legal SQLite page sizes such as
8 KiB while still storing their images in 4 KiB Graft chunks.

For file artifacts, staging records inline blobs or external payload pointers.

## Checkout And Switch Flow

```text
resolve target commit
  -> compare target tree to current worktree/index state
  -> acquire SQLite replacement locks and normalize/remove WAL sidecars
  -> materialize tracked SQLite snapshots and files
  -> update HEAD or branch
```

Conflicting local work blocks checkout unless the command is explicitly forced.
An active SQLite writer also blocks physical database replacement.

## Merge Flow

```text
base tree + ours tree + theirs tree
  -> path-level merge
  -> SQLite row/schema analysis for database paths
     (direct parser fast path or materialized SQLite compatibility path)
  -> auto-merge compatible database changes
  -> store unresolved stages in the index
  -> merge --continue writes the merge commit
```

File artifacts use conservative file-level conflict handling unless a resolver
understands the format.
