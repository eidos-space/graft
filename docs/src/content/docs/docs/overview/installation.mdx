---
title: Installation
description: Install the Graft CLI, build from source, and add the Node.js SDK.
---

import { Aside } from "@astrojs/starlight/components";

The fastest way to try Graft is the CLI. Applications can later use the
resident Node.js/Electron SDK for an in-process integration.

## Install The CLI

Install the version documented by this site:

```bash
curl -fsSL https://graft.eidos.space/install.sh \
  | GRAFT_VERSION=0.13.0 sh
```

Then check the binary:

```bash
graft --version
# graft-cli 0.13.0
```

<Aside type="note" title="Latest release">
  Omit `GRAFT_VERSION=0.13.0` to install the latest published release.
</Aside>

Prebuilt archives are also available from the
[GitHub releases page](https://github.com/eidos-space/graft/releases).

## Build The CLI From Source

From the repository root:

```bash
cargo build -p graft-cli --bin graft --release
```

The binary is written to:

```text
target/release/graft
```

Add it to your shell path:

```bash
mkdir -p ~/.local/bin
ln -sf "$(pwd)/target/release/graft" ~/.local/bin/graft
hash -r
graft --help
```

## Build The Docs

The documentation site lives in `docs/`:

```bash
cd docs
pnpm install
pnpm build
```

## Install The Node.js And Electron SDK

```bash
pnpm add @eidos.space/graft
```

The npm package provides long-lived in-process repository sessions and prebuilt Node-API 8
binaries. Continue with the [Node.js and Electron SDK guide](/docs/guides/node-electron-sdk/).

## Next

Run the [CLI quickstart](/docs/quickstart/cli/) first, then use the
[Node.js and Electron SDK guide](/docs/guides/node-electron-sdk/) when an
application needs a resident repository session.
